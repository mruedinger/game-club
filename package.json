{
  "name": "game-club",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test:e2e": "playwright test",
    "db:migrate:dev": "wrangler d1 migrations apply game-club-dev --remote --env dev",
    "db:migrate:prod": "wrangler d1 migrations apply game-club --remote --env production",
    "db:bootstrap:dev": "wrangler d1 execute game-club-dev --remote --file schema/bootstrap.sql --env dev",
    "db:bootstrap:prod": "wrangler d1 execute game-club --remote --file schema/bootstrap.sql --env production"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.12",
    "astro": "^5.16.6",
    "jose": "^6.1.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "wrangler": "^4.58.0"
  }
}
